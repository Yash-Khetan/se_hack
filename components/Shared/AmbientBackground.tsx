import React from 'react';
import { View } from 'react-native';

/**
 * AmbientBackground is currently disabled as per user request to 
 * remove background animations. 
 * Re-enable by adding back to GradientBackground.tsx
 */
export default function AmbientBackground() {
  return <View />;
}
